#include <stdio.h>
#include <stdlib.h>
#include "list.h"

int main() {
  /*struct node n;
  struct node *p = &n;
  p = NULL;
  int i;
  printf("\nPrinting empty list created:\n");
  print_list(p);
  printf("Inserting value 1 to 9: \n");
  for (i = 1; i < 10; i++) {
    p = insert_front(p, i);
  }
  print_list(p);
  p = removeNode(p, 9);
  print_list(p);
  printf("Freeing all values: \n");
  p = free_list(p);
  print_list(p);*/
  return 0;
}
